services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${IMG_TOOLS_PORT:-8080}:80"
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
    restart: unless-stopped
